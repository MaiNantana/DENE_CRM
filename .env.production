# Public VITE_ vars embedded in the frontend bundle at build time.
# These are NOT secrets — they appear in the compiled JS and in LINE's liff.line.me URLs.
# Real secrets (LINE tokens, DB password, AUTH_SECRET) stay in .env.local which is
# deployed separately by the deploy script and never committed.
VITE_LIFF_ID=2010272730-I91w8i30
VITE_LIFF_ID_DENE=2010272730-I91w8i30
VITE_LIFF_ID_KEFERA=2010432725-CJdG7OzF
